(function() {

    class Animation {

        constructor(
            hostElement,
            sprite
        ) {
            this.hostElement = hostElement;
            this.sprite = sprite;
        }

        run(name) {
            
        }

        tick() {

        }

    }

    window.Animation = Animation;

})();